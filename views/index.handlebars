{{!-- navbar --}}
<nav class="navbar navbar-dark bg-dark">
    <form action='/' method='get'>
        <button type='submit' class="btn btn-outline-light my-2 my-sm-0 btn-signin">
            <i class="fa fa-paper-plane" aria-hidden="true"></i>&nbsp;&nbsp;Home
        </button>
    </form>
    <a class="navbar-brand" href="#"></a>
    <form action="add" method='get'>
        <button type='submit' class="btn btn-outline-light my-2 my-sm-0 btn-signin">
            <i class="fa fa-paper-plane" aria-hidden="true"></i>ADD QUESTION
        </button>
    </form>
</nav>

{{!-- jumbotron --}}
<div class="jumbotron jumbotron-fluid" style="background-color: #401f68; font-family: 'Source Serif Pro', serif;">
  <div class="container">
    <h1 class="display-4" style="color: white">Welcome to Fist-Of-Five!</h1>
    <hr style="background-color: white;">
    <p class="lead" style="color: white;">To start, you can rate a topic or click "Add Question" to add a new topic!</p>
  </div>
</div>

<section class="container dashboard wrapper">
    <div class="row">
        <div class="col">
            <div class="col-12">
                {{#each fist5}}
                    <div class="card text-center">
                        <div class="card-header" style="background-color: #836EAA; padding: 30px; font-family:'Playfair Display', serif; font-size: 35px">
                        {{this.topic}}
                        </div>
                        
                        <br>

                        <div class="card-body" style="box-shadow: 10px 10px 5px grey">
                            <form action='/' method='post'>
                                <div class="form-group">
                                    <label for="priority"><span class="required">*</span></label>
                                    <input type="submit" id="f1" name="count" value="{{this.id}}.f1">
                                    <label>Fist 1</label>
                                    <input type="submit" id="f2" name="count" value="{{this.id}}.f2">
                                    <label>Fist 2</label>
                                    <input type="submit" id="f3" name="count" value="{{this.id}}.f3">
                                    <label>Fist 3</label>
                                    <input type="submit" id="f4" name="count" value="{{this.id}}.f4">
                                    <label>Fist 4</label>
                                    <input type="submit" id="f5" name="count" value="{{this.id}}.f5">
                                    <label>Fist 5</label>
                                </div>
                            </form>

                            {{this.favg}}

                            {{this.createdAt}}
                        </div>
                    </div>
                    <br><br>
                {{/each}}
            </div>
        </div>             
    </div>
</section>

<form action ="history" method='get' >
 <button type='submit' class="btn btn-outline-secondary btn-signin offset-8">
    <i class="fa fa-paper-plane" aria-hidden="true"></i>See History
</button><br>
</form>  

<!-- Start Banner Area -->
        <section class="banner-area relative" style="background-color: #401f68; padding: 30px">
            <div class="overlay overlay-bg"></div>
            <div class="container">
                <div class="row fullscreen justify-content-center align-items-center">
                    <div class="col-lg-8">
                        <div class="banner-content text-center">
                            {{!-- <p class="text-uppercase text-white">We work hard, we result perfect</p> --}}
                            <h1 class="text-uppercase text-white" style="font-family: 'Source Serif Pro', serif;">By: Northwestern & Trilogy</h1>
                            {{!-- <a href="#" class="primary-btn banner-btn">Explore Us</a> --}}
                        </div>
                    </div>
                </div>
            </div>
        </section>